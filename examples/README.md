# Examples

Small standalone scripts demonstrating Reachy Mini motion.

- `wave_hello.py` — wave the antennas and nod hello on real hardware.
- `simulation_demo.py` — move the head and wiggle antennas against a local simulation.

Run with the project virtual environment active:

```bash
python examples/wave_hello.py
```
